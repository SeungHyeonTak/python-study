python 함수
Dictionary
key와 value를 쌍으로 갖는 자료형
dict_ = {key: value ....}
관련된 정보를 연관 시킨다.
dict_[0] = 'a'
이렇게 하면 0이 key가 되고 a가 value가 된다.
del 이라는 keyword로 key를 입력하여 지울 수 있다.

딕셔너리 메서드
딕셔너리만 사용할 수 있는 함수
dict.values() >> 딕셔너리에서 값만 뽑아  돌려준다.
dict.keys() >> key만 뽑아온다.
dict.items() >> 딕셔너리에서 키와 값 쌍을 뽑아 돌려준다.
기본적으로 3가지가 있다.

function(함수)
내장 함수 - 기본적인 print() input() 이런거
모듈의 함수 - import를 이용해 가져다 쓸수 있는 함수
사용자 정의 함수 - 우리가 직접만들어서 사용하는 함수
def 함수이름(인자1...)
    실행할 명령1
    ......
    return 결과

함수를 쓰는 이유
다시 사용할 수 있다.
코드를 관리하기 쉽다.
조립해서 사용할 수 있다.

여러개 돌려주기
return 에서 ,를 사용해 여러개의 값을 돌려받을 수 있다.
여러개를 돌려줄 때는 튜플로 묶어서 전달하기 때문에
하나로 전달 받는 방식이다.

모듈(Module)
비슷한 함수들을 모아둔 파일이다.
import 키워드로 모듈을가져온다.
.을 이용해 함수를 사용한다.

랜덤 모듈(random)
난수발생
무작위와 관련된 함수를 포함
random.choice() >> 리스트의 값 중 하나를 랜덤하게 선택
random.sample(list, int(number)) >> 리스트의 값중에서 지정한 개수만큼 랜덤하게 선택 가능
random.randint(8,10)>> 특정 범위의 정수 중 하나를 랜덤하게 선택

Object -객체


